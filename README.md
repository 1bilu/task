Step 1: Create a Basic Flask Application
   First, let's create a simple Flask application.

step 2: Create an 'app.py' file with a basic Flask application:

Step 3: Write Unit Tests for the Flask Application
   Next, write a couple of unit tests for the Flask application.

Step 4: Create a tests directory with a test_app.py file: 
          mkdir tests/test_app.py

Step 5: pip install pytest

step 6: pytest

Step 7: Set Up GitHub Actions for CI/CD Pipeline
     git init
     git add .
     git commit -m "Initial commit"
     git branch -M main

step 8: Create a GitHub repository and push the code to it.




